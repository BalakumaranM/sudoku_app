enum GameMode { shapes, colors, numbers, custom, planets, cosmic }

enum Difficulty { easy, medium, hard, expert, master }

enum CellHighlight { none, selected, related, matching, block, hintTarget, hintRelated, hintEliminated, hintNumberInstance }

enum SudokuSize { mini, standard }
